meta {
  name: Refresh Token
  type: http
  seq: 3
}

post {
  url: {{SERVICE_URL}}/api/v1/auth/refresh-token
  body: json
  auth: inherit
}

body:json {
  {
    "refresh_token": "token"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
